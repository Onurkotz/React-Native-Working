import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    height: 40,
    margin: 10,
    borderWidth: 2,
    borderRadius: 10,
    padding: 10,
    backgroundColor: "aqua"
  },
});
